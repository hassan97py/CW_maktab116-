# from setuptools import setup, find_packages

# setup(name='q4',version='0.1',packages=find_packages(),)


from setuptools import setup, find_packages

setup(
    name='utils',
    version='0.1',
    packages=find_packages,
)